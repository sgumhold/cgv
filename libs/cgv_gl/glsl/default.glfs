#version 150

uniform float gamma = 2.2;

in vec4 color_fs;

out vec4 frag_color;

void main()
{
	frag_color = color_fs;
	// perform inverse gamma correction
	frag_color.rgb = pow(frag_color.rgb, vec3(gamma));
}
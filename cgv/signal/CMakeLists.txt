
file(GLOB_RECURSE SOURCES RELATIVE "${CMAKE_CURRENT_SOURCE_DIR}" "*.cxx")
file(GLOB_RECURSE PPP_SOURCES RELATIVE "${CMAKE_CURRENT_SOURCE_DIR}" "*.ph")

install(DIRECTORY ${PPP_INSTALL_DIR} DESTINATION ${CGV_INCLUDE_DEST}/signal FILES_MATCHING PATTERN "*.h")

cgv_create_lib(cgv_signal CORE_LIB SOURCES ${SOURCES} PPP_SOURCES ${PPP_SOURCES}
        DEPENDENCIES cgv_type)

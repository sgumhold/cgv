#pragma once

#include <array>

#include <cgv/math/functions.h>

#include "color.h"

namespace cgv {
namespace media {
namespace colors {

static const cgv::rgb black = { 0.0f, 0.0f, 0.0f };
static const cgv::rgb blue = { 0.0f, 0.0f, 1.0f };
static const cgv::rgb brown = { 0.3f, 0.1f, 0.0f };
static const cgv::rgb cyan = { 0.0f, 1.0f, 1.0f };
static const cgv::rgb gray = { 0.5f, 0.5f, 0.5f };
static const cgv::rgb green = { 0.0f, 1.0f, 0.0f };
static const cgv::rgb magenta = { 1.0f, 0.0f, 1.0f };
static const cgv::rgb red = { 1.0f, 0.0f, 0.0f };
static const cgv::rgb white = { 1.0f, 1.0f, 1.0f };
static const cgv::rgb yellow = { 1.0f, 1.0f, 0.0f };

} // namespace colors
} // namespace media
} // namespace cgv

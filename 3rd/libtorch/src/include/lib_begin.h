#pragma once

// Because libtorch can only be used as shared library, the usual guard with
// conditional definition would always result in the following
#define CGV_EXPORTS

#include <cgv/config/lib_begin.h>


// dataset to load (empty means built-in placeholder dataset)
type(tubes):dataset_fn=""

// renderer to use (0=rounded_cone, 1=spline_tube)
type(tubes):renderer=1

// persistant state
persistent
type(tubes):instant_redraw_proxy=false
